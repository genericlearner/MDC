function main() => void
{
  local x : int;
  local y: int;
  local z: int;
  
 y := 1 + 2 * 3;                                   // 3.1, 5.1
                                             // 3.4
    x := 10;
	if (x > y + 10)                                  // 3.2, 5.1   
    then		
      x := x+1;                                // 3.4, 5.1 
    else                                    // 3.4, 5.1
    ;
    z := 9;                                           // 3.1
    while(z<=10){                                     // 3.3, 5.1
      write(z); 
	    z := z+1;                                     // 3.4
    }; 
    read(x);
    write(x);
}